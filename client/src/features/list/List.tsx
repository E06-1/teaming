import React from 'react';
import type {teaming} from "../../../../types";

interface ListProps {
    id: teaming.ListId;
}

function List({id}: ListProps) {
  return (
    <div className="List">
    </div>
  );
}

export default List;
